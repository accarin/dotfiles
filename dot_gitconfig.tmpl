[user]
    name  = {{ .git_name | quote }}
    email = {{ .git_email | quote }}

[core]
    editor = code

[init]
    defaultBranch = main

[pull]
    rebase = true

[alias]
    st  = status
    co  = checkout
    lg  = log --oneline --graph --decorate

{{- if or (eq .machine_type "server") (eq .machine_type "wsl") }}
# On servers/WSL, don't use a credential helper - use SSH keys only
[credential]
    helper =
{{- end }}
